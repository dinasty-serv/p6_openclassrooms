<div class="card h-100">
    <h4 class="card-header">Catégories</h4>
    <div class="card-body">
        <ul>
            {% for item in category %}
            <li><a href="{{ path('app_trick_view_all',{'slug_category': item.slug}) }}"> {{ item.name}} </a></li>
            {% endfor %}
        </ul>
    </div>

</div>